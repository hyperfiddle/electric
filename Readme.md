# Fabric

```bash
git submodule update --init --recursive
yarn
yarn run dev    # client build and asset server
# go to http://localhost:9630/builds and start watch
clj -A:server  # launch server repl, go to user.clj and start server
```
